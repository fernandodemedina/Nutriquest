# NutriQuest — plan de performance (carga lenta)

- [ ] (B) Mostrar mapa inmediatamente sin esperar a `fetch()` completo de `glosario.json`.
- [ ] Completar el resto del mundo en background (“hydrate”).
- [ ] (A) Cachear `data/glosario.json` en `localStorage` para que el 2º arranque sea sin red.
- [ ] Validar que en Network panel haya 0 descargas en el 2º arranque.
- [ ] Verificar que no se rompa el guardado/progreso al actualizar glosario.

