# TODO - Corregir “conceptos anteriores” que aparecen en mundos

- [ ] Confirmar causa: si es de `minijuegos` (distractores usando mal el mundo) o de `Enciclopedia`/filtro
- [ ] Verificar asignación `mundo` en `data/glosario.json` (ej: “NUTRIENTE”) contra lo esperado
- [x] Corregir dependencia `nqNormalize` (ahora está en `minigames.js` pero se usa en `ui.js`)

- [ ] Reforzar reset de progreso al cambiar glosario (evitar reusar `unlockedTerms` / `completedLevels` de guardados viejos)
- [ ] Probar navegando: Mundo 1 → Mundo 2 y revisar distractores + Enciclopedia

